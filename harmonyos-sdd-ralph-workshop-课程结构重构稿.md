# 使用 AI 进阶能力实现较为复杂的需求

> 39 页 PPT-ready 内容稿与讲师备注｜架构重构版｜暂不制作 PPT

这版不从“提示词技巧”开场，而是先回答三个问题：AI 开发方式发展到哪一步、为什么复杂需求仍然容易失败、工程师怎样把一次 AI 完成变成可验收交付。随后用两个案例把同一套方法走两遍：MDM 应用从原始需求图到逐 Story 交付；大型远控项目从陌生代码勘察、跨平台调研到 GPU 最小能力穿刺与设备验收。

> **案例二当前状态**：第 26–37 页只冻结方法、讲述顺序、判断问题和证据槽位，不预写具体项目事实。代码、API、日志、Session、性能数字和最终结果由负责该案例的其他 Session 回填。

## 课程叙事与时间

| 时间 | 页码 | 模块 | 学员最终获得 |
|---|---:|---|---|
| 00–24 分钟 | 1–8 | 从 Prompt 到 Loop：为什么复杂需求需要工程闭环 | 能区分 Chat、IDE、Agent、Cowork、Loop 的角色，并识别“AI 完成”的假象 |
| 24–72 分钟 | 9–25 | 案例一：HarmonyOS MDM 应用 | 能把原始需求变成 PRD、RFC、Story、验证门和证据包 |
| 72–112 分钟 | 26–37 | 案例二：30 万行以上开源远控项目 | 能在陌生大仓中控制上下文、做跨平台调研、能力穿刺和证据驱动排障 |
| 112–120 分钟 | 38–39 | 迁移与收束 | 带走一张可复用的复杂需求作战图和七个判断问题 |

## 页面统一规则

每页顶部必须是一句结论型标题；正文尽量回答四件事：

1. **怎么做**：工程动作是什么。
2. **怎么判断**：用什么证据判定对错。
3. **不对怎么办**：失败时退回哪一层，不让 AI 继续盲改。
4. **这一页产出**：留下什么结构化结果供下一步使用。

事实标签：`CURRENT` 表示当前仓库事实；`SESSION FACT` 表示本地真实 Session 记录；`TEACHING` 表示课堂结构化表达；`PLACEHOLDER` 表示等待用户补充的截图或视频。

---

## 第 1 页｜复杂需求的目标不是“AI 写完”，而是团队能够证明它交付了

### PPT 内容

**使用 AI 进阶能力实现较为复杂的需求**  
副标题：从需求、实现、调试到设备验收的闭环

画面中心只保留一条主线：

```text
模糊需求 → 可验证规格 → 有边界 Story → AI 执行 → 设备证据 → 可交付结果
```

底部放两个真实案例：

- HarmonyOS MDM：从一张原始需求图，走到逐 Story 验收。
- HarmonyOS 远控：在 30 万行以上开源代码中，把 CPU 视频链路迁移到硬解/GPU 送显。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：后续每个案例都沿“目标—真相源—Story—执行—证据”推进。
- 怎么判断：课堂结束后，学员不是记住某个 Prompt，而是能回答：**为什么 AI 是对的；如果不对，下一步查哪里。**
- 不对怎么办：如果一页只剩工具操作或正确答案，没有判断依据和失败路径，就回到真实问题与证据重讲。
- 本页产出：统一课程目标——可信结果，而不是代码数量。

### 讲师备注

- 开场不要先讲 Ralph，也不要先讲目录规范。先问学员：“AI 回复已经完成，这句话值多少钱？”
- 等学员说出测试、构建、真机、业务状态后，再揭示右侧的证据链。
- 强调两个案例难点不同：MDM 难在需求/权限/状态/事务；远控难在陌生大仓、跨平台和多阶段图形链路。但判断 AI 是否正确的方式相同。

### 演示 / 素材

- `PLACEHOLDER`：左侧放 MDM 原始需求图，右侧放远控视频卡顿截图或 5 秒视频封面。

---

## 第 2 页｜AI 开发不是一种模式：Chat、IDE、Code Agent、Cowork 解决的是不同范围

### PPT 内容

用一条由左向右扩展的工作面说明能力边界：

| 方式 | AI 能看到什么 | 适合做什么 | 主要风险 |
|---|---|---|---|
| Chat / Chatbox | 用户粘贴的片段 | 解释、讨论、草拟方案 | 上下文不完整，答案难落地 |
| IDE Assistant | 当前工程、选中代码、Diff | 局部编码、重构、代码解释 | 容易把局部正确当成整体正确 |
| Code Agent | 仓库、终端、Git、测试工具 | 多文件实现、构建、测试、修复 | 可执行范围扩大，错误也会放大 |
| Cowork / Desktop Agent | 文件、应用、跨工具工作流 | 资料整理、跨应用交付、通用知识工作 | 权限、意图理解和外部动作风险更高 |

页面结论：**演进的不是“对话框更大”，而是 AI 能操作的环境和承担的闭环更完整。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先按任务范围选工作面，不默认所有问题都交给自治 Agent。
- 怎么判断：需要读仓、执行命令、观察环境反馈时，Chat 不够；只改一个确定文件时，长时 Agent 又未必必要。
- 不对怎么办：如果 Agent 在范围外探索，退回 IDE/只读勘察；如果 Chat 结论无法验证，升级为能接触真实环境的工具链。
- 本页产出：一张“任务范围—AI 形态”选择表。

### 讲师备注

- 这里可以沿用之前 Anthropic “Chat → Code → Cowork” 的视觉思路，但要补上 IDE 这一层，方便研发同学对应日常工作。
- 不要讲成产品发布史，也不要暗示新方式替代旧方式。正确表达是：**同一个复杂需求会在几种工作面之间切换。**
- 例子：需求澄清可以在 Chat；局部修改在 IDE；跨文件实现用 Code Agent；整理验收材料或跨应用汇总可用 Cowork。

### 演示 / 素材

- 建议自绘四列卡片，不直接拼产品宣传图。
- `[Sources]` https://www.anthropic.com/webinars/future-of-ai-at-work-introducing-cowork  
  `[Sources]` https://code.claude.com/docs/en/platforms

---

## 第 3 页｜真正的跃迁是从一次 Prompt，变成“执行—观察—修正”的 Agent Loop

### PPT 内容

纵向展示四代工作方式：

```text
Prompt：问题 → 回答
Workflow：固定步骤 A → B → C
Agent Loop：计划 → 行动 → 观察 → 调整 → 再行动
Harness：规格 + 工具 + 证据 + 停止条件，约束 Agent Loop
```

右侧标一句：

> Prompt 决定一次回答；Loop 决定如何继续；Harness 决定什么叫完成。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：把构建、测试、安装、日志采集等确定步骤做成 Workflow；把需要探索和判断的部分交给 Agent Loop。
- 怎么判断：Agent 每一次循环都必须获得新的环境反馈，而不是反复改 Prompt 或重复猜测。
- 不对怎么办：连续两轮没有新增证据，暂停循环，检查规格、工具可见性或判断标准，而不是继续消耗上下文。
- 本页产出：课程后续统一使用的“受控循环”模型。

### 讲师备注

- 先动画显示 Prompt 的单箭头，再补上工具和观察形成闭环，最后在外面套上 Harness。
- 可举构建失败为例：回答“应该能编译”不是反馈；真正运行构建，拿到错误码和日志，才形成下一轮输入。
- Anthropic 对 workflow 与 agent 的区分可作为理论依据：workflow 走预定义路径，agent 根据反馈动态决定工具和步骤。

### 演示 / 素材

- 可参考社区的 `SPEC → CODE → TEST` 图，但建议重绘并补上“证据”和“停止条件”。
- `[Sources]` https://www.anthropic.com/research/building-effective-agents  
  `[Sources]` https://addyosmani.com/blog/good-spec/  
  `[Sources]` https://www.philschmid.de/inner-loop-vs-outer-loop

---

## 第 4 页｜当前趋势是“人负责方向与判断，AI 承担越来越多执行”

### PPT 内容

页面放一个 2×2 分工图：

| | 方向/决策 | 执行/验证 |
|---|---|---|
| 人 | 需求取舍、风险、验收口径、是否发布 | 关键体验与高风险确认 |
| AI | 提出备选、暴露未知项 | 搜索、改动、测试、构建、整理证据 |

旁边放研究快照：在 Anthropic 对约 40 万次 Claude Code 会话的研究中，用户承担约 70% 的规划决策，Claude 承担约 80% 的执行决策。

页面结论：**工程师的价值从“每一行都亲手写”转向“定义问题、设计反馈、辨别证据”。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：由人冻结需求取舍、风险和验收口径，让 AI 承担搜索、实现、运行验证和整理证据。
- 怎么判断：当 AI 对需求取舍、架构边界或发布结论做了决定，必须能指出它依据的规则和证据。
- 不对怎么办：如果决策依据只来自模型自述，收回决策权；让 AI 提供选项、影响面和验证结果，由人做门控。
- 本页产出：人机责任边界。

### 讲师备注

- 明确这是研究样本中的行为分布，不是所有团队的固定比例。
- 顺势引出后面的核心：AI 可以多执行，但“什么叫正确”不能外包给 AI 自己。

### 演示 / 素材

- 自绘人机分工矩阵；不要把百分比画成“效率提升”。
- `[Sources]` https://www.anthropic.com/research/claude-code-expertise

---

## 第 5 页｜复杂需求最危险的结果不是完全错误，而是“几乎正确”

### PPT 内容

用四张风险卡呈现：

1. **一致性差**：规格、代码、测试、文档在不同 Session 中逐渐分叉。
2. **局部正确**：某个函数或页面工作，但权限、签名、状态流或其他平台被破坏。
3. **验证偏差**：AI 写出的测试只证明了自己的实现，没有证明用户需求。
4. **完成幻觉**：测试绿、工具返回成功或 UI 正常，被误写成业务交付。

数据侧栏：

- Stack Overflow 2025 调查中，66% 的受访者把“AI 方案几乎正确但不完全正确”列为最大挫折；45% 认为调试 AI 代码更耗时。
- METR 在一项早期 2025 年、16 名有经验开源开发者和 246 个真实任务的随机实验中，测得使用当时 AI 工具平均慢 19%；参与者事前却预计会快 24%。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：把“一致性、范围、验证、完成”分别设门，不用一个测试绿覆盖全部问题。
- 怎么判断：至少存在一个独立于实现的 oracle——原始需求、系统 getter、协议事实、设备日志或人工体验。
- 不对怎么办：判断不出时标记 `UNKNOWN`，列出缺失证据，不允许 AI 补一个看似合理的结论。
- 本页产出：复杂需求的四类失败清单。

### 讲师备注

- 数据用于打破“AI 一定更快”的直觉，不要把单项研究扩大成永恒结论；METR 研究明确是特定时间、工具和样本的快照。
- 可以让学员举一个“几乎正确”的例子，后面两个案例都要回扣这四类风险。

### 演示 / 素材

- 建议用自绘风险卡和数字大字报，不直接截网页。
- `[Sources]` https://survey.stackoverflow.co/2025/ai  
  `[Sources]` https://metr.org/Early_2025_AI_Experienced_OS_Devs_Study-paper.pdf

---

## 第 6 页｜“AI 完成”到“可交付”之间，至少隔着五道验证门

### PPT 内容

画成从左到右的五道门：

```text
代码门 → 测试门 → 构建门 → 环境门 → 业务门
```

每道门回答不同问题：

| 门 | 证明什么 | 不能证明什么 |
|---|---|---|
| 代码 | 变更存在、范围可审查 | 行为正确 |
| 测试 | 已知规则被执行 | 真机、权限、未知场景正确 |
| 构建 | 工程可产出 | 可安装、可运行 |
| 环境 | 签名、安装、权限、设备链路可用 | 业务状态正确 |
| 业务 | 用户目标与系统事实满足 | 后续长期稳定性 |

顶部结论：**任何一扇门通过，都不能替代后一扇门。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：Story 验收时逐门记录命令、结果和证据路径。
- 怎么判断：只有所有必需门都通过才是 PASS；无法执行某门就是 UNKNOWN，不是默认通过。
- 不对怎么办：失败退回对应层，例如安装失败先查签名和环境，不让 AI继续改业务逻辑。
- 本页产出：`PASS / FAIL / UNKNOWN` 验收状态模型。

### 讲师备注

- 用 MDM 案例预告：HAP 构建、设备连接、管理员激活都通过，导航 E2E 仍因测试 ID 缺失失败。这就是“完成不等于交付”的真实证据。
- DORA 的研究可用来补一句：AI 更像放大器，底层交付系统越弱，生成越快越可能放大不稳定性。

### 演示 / 素材

- 后续 P24 放真实报告，这一页只建立读法。
- `[Sources]` https://dora.dev/research/2025/dora-report/  
  `[Sources]` https://dora.dev/insights/balancing-ai-tensions/

---

## 第 7 页｜Ralph Loop 解决“持续推进”，但规格、验证和停止条件必须由工程系统提供

### PPT 内容

先放社区版 Ralph Loop：

```text
选择一个小任务 → 新鲜上下文执行 → 写回代码/进度 → 再选择下一个任务
```

再在外面加四个工程护栏：

- `Spec`：每轮要满足的行为与不变量。
- `Scope`：允许和禁止修改的路径。
- `Verifier`：测试、构建、设备或系统事实。
- `Stop`：PASS、FAIL、UNKNOWN、重试上限和人工升级条件。

页面结论：**Ralph 是循环机制，不是正确性来源；没有 verifier 的循环，只会更稳定地重复错误。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：一次只取一个 Ready Story；每轮从文件系统读取规格和上轮证据；结束时写回状态与下一步。
- 怎么判断：新一轮是否缩小了未知项、产生了可复现证据、没有越过范围。
- 不对怎么办：重复失败两轮、修改面持续扩大、或证据互相冲突时停止自治循环，进入人工评审/重新设计。
- 本页产出：本课使用的“受控 Ralph”——`Story + fresh context + evidence + stop condition`。

### 讲师备注

- 不需要讲 Ralph 的所有实现细节。只讲它为什么适合长任务：任务小、上下文新、状态写入外部文件。
- 同时指出它不能替代需求判断、架构选择和业务验收。
- 可参考社区 Ralph 图，但建议重绘；社区图有趣，工程护栏必须是本课自己的重点。

### 演示 / 素材

- `PLACEHOLDER`：可放 Ralph 社区图的缩略图，右侧放本课重绘的受控循环。
- `[Sources]` https://github.com/coreyepstein/ralph-methodology  
  `[Sources]` https://www.anthropic.com/research/building-effective-agents  
  `[Sources]` https://www.langchain.com/blog/improving-deep-agents-with-harness-engineering

---

## 第 8 页｜接下来用两个案例验证同一套方法，而不是再讲一遍概念

### PPT 内容

左右双案例地图：

| 案例一：MDM 应用 | 案例二：大型远控 |
|---|---|
| 入口：原始需求图 | 入口：CPU 高、视频卡顿 |
| 首要不确定性：需求、状态、权限 | 首要不确定性：代码、平台、图形链路 |
| 先做：PRD → RFC → Story | 先做：代码勘察 → 对标调研 → 能力穿刺 |
| 验收：逐 Story + 真机 + 系统状态 | 验收：帧级 Trace + 图像 + 性能 + 回退 |

中间用同一条方法连接：

```text
明确结果 → 找真相源 → 识别未知 → 最小穿刺 → 拆 Story → 受控执行 → 证据验收 → 写回文档
```

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：进入每个案例前标记当前入口、最大未知和下一道验证门。
- 怎么判断：如果后续每一步都能映射回这条主线，课程是连续的；如果只剩某个 Prompt 或工具操作，就是跑偏。
- 不对怎么办：案例细节开始淹没主线时，回到中间这条八步路径，说明当前位于哪一步、下一步为何存在。
- 本页产出：整门课导航。

### 讲师备注

- 告诉学员，第一案重点是“如何把需求变成可执行系统”；第二案重点是“如何在未知系统中先获得最小真相”。
- 两案结束都要回答同一句话：我们凭什么认为 AI 是对的？

---

## 第 9 页｜案例一的复杂性来自业务、权限、状态和设备共同作用

> **MDM 第 9–25 页详细版以 `case-materials/mdm/07-PPT第9-25页内容与备注.md` 为准。** 详细版已经按“资产同步 → 方案推导 → RFC → Story → Ralph → 测试验收”重排，并引用同目录六份真实课堂文档；本文件以下内容保留为课程摘要，制作 PPT 时应使用详细版替换。

### PPT 内容

案例背景：HarmonyOS 2in1 安全管理中心，包含安全总览、防火墙、日志、外设、权限、身份鉴别等模块，并依赖 MDM 权限、企业管理员、签名和真机状态。

把“复杂”拆成四层：

1. 页面能展示和操作。
2. ViewModel / Service / Repository 状态一致。
3. 系统 API、权限与管理员状态真实生效。
4. 多用户、进程时序、失败恢复仍满足业务规则。

页面结论：**这不是多写几个页面，而是一条跨 UI、业务、系统和设备的状态链。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先画边界图，列出每层真相源和责任人，再开始写 PRD。
- 怎么判断：任何用户可见状态，都要能追溯到业务模型和系统事实。
- 不对怎么办：如果只能证明 UI 变了，先标记“表面通过”，不要进入完成状态。
- 本页产出：MDM 影响面图。

### 讲师备注

- 先展示应用主界面或模块总览，不马上进入代码。
- 提醒学员，签名/管理员不是课外环境问题，而是业务能力能否成立的一部分。

### 演示 / 素材

- `PLACEHOLDER`：应用主界面、功能导航、企业管理员激活状态截图。
- 本地依据：`C:/Users/mu/Desktop/code/security_tool/AGENTS.md` 与 `docs/03-模块设计/`。

---

## 第 10 页｜原始图片不能直接喂给 AI 开发，要先变成 PRD、RFC 和 Story

### PPT 内容

用四层漏斗展示：

```text
原始图片/口头需求
        ↓
PRD：用户要发生什么
        ↓
RFC：系统如何保证它发生
        ↓
Story：这一轮 AI 只交付什么
```

四层各自保留不同信息：

- 原始材料：页面、文案、操作意图、明显约束。
- PRD：角色、场景、行为、异常、待决策项。
- RFC：模块边界、状态模型、接口、权限、时序、不变量。
- Story：范围、依赖、验收标准、证据和完成定义。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：AI 先做“提取与提问”，不能从截图直接跳到实现方案。
- 怎么判断：PRD 不出现类名和函数；RFC 不遗漏状态/失败语义；Story 可在一个上下文内实现和验收。
- 不对怎么办：如果同一句话在三份文档中含义不同，停止编码，先完成术语表与决策记录。
- 本页产出：`source/ → prd/ → rfc/ → stories/` 文档链。

### 讲师备注

- 现场可展示原始需求图 20 秒，让学员说出其中至少三个歧义，再揭示 PRD 中的“待决策项”。
- 核心不是文档越多越好，而是让不同层的问题在正确的位置被解决。

### 演示 / 素材

- `PLACEHOLDER`：原始需求图。
- `PLACEHOLDER`：PRD、RFC、Story 三份文档的真实目录截图。

---

## 第 11 页｜PRD 先冻结用户行为：谁在什么状态下做什么，失败时看到什么

### PPT 内容

以“系统账号变化后，防火墙策略同步”为例，PRD 不先写技术方案，而是写行为矩阵：

| 场景 | 前置状态 | 用户/系统动作 | 期望结果 | 失败呈现 |
|---|---|---|---|---|
| 新增账号 | 当前为公开/私密模式 | 系统新增用户 | 当前模式自动覆盖最新账号集合 | 不显示虚假成功 |
| 删除账号 | 已有账号策略 | 系统删除用户 | 失效账号从适用集合移除 | 读取失败时不误删 |
| 自定义模式 | 用户明确选定账号 | 账号集合变化 | 不自动解释为“所有账号” | 保留用户选择 |

页面上方列三个待决策问题：

- “全部账号”是动态集合，还是保存时的快照？
- 系统事件到达但账号列表未更新时，听谁的？
- 失败后是重试、保持旧状态，还是部分提交？

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：把模糊词转换为场景、状态和可观察结果。
- 怎么判断：每个行为都有正常、边界和失败分支；不同角色对同一句话不会有不同解释。
- 不对怎么办：尚未决策的项进入 `Open Questions`，不得让 AI 自行选择并隐藏在代码里。
- 本页产出：行为矩阵与待决策清单。

### 讲师备注

- 让学员先判断“全部账号”是否包含未来新增账号。不同答案会导致完全不同的数据模型，这正是 PRD 必须先解决的原因。
- 强调 UI 文案不能代替业务语义。

---

## 第 12 页｜RFC 冻结的是系统不变量和责任边界，不是实现细节堆积

### PPT 内容

把 PRD 的行为翻译成 RFC：

**核心不变量**

1. 公开/私密模式作用于“最新可见账号集合”。
2. 自定义模式不是动态 ALL。
3. 账号读取为空或失败时，不允许据此删除已有策略。
4. UI 刷新是同步结果，不是修复手段。

**责任边界**

```text
SystemUserProvider：读取系统账号真相
AccountChangeCoordinator：处理事件、等待稳定快照、决定是否分发
Mode Handler：按模式生成业务变更
Repository：持久化与事务
UI/ViewModel：订阅结果并刷新
```

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：为每个不变量指定真相源、所有者和失败语义。
- 怎么判断：同一规则只在一个责任层做决定；UI、事件回调和缓存都不能成为多套真相源。
- 不对怎么办：出现两层都在“补偿”同一问题时，先重新划分所有权，再修改代码。
- 本页产出：状态模型、责任图、非目标范围。

### 讲师备注

- 重点讲“事件不是事实”“UI 不是修复”。这两句话会在后面的真实用户 ID 问题里得到验证。
- 可让学员指出：如果在页面 onShow 里重新加载所有策略，看似好了，为什么仍然是错误修复？答案是后台状态和持久化仍可能不一致。

### 演示 / 素材

- 本地依据：`C:/Users/mu/Desktop/code/security_tool/docs/superpowers/plans/2026-07-02-firewall-account-reconcile.md`。

---

## 第 13 页｜Feature 要拆成可独立验证的 Story，而不是按页面或文件平均切块

### PPT 内容

示例 Story 地图：

```text
S1 读取真实账号集合
   ↓
S2 账号新增时等待稳定快照
   ↓
S3 按当前模式计算策略变更
   ↓
S4 事务写入与失败回滚
   ↓
S5 UI 刷新与用户可见反馈
   ↓
S6 真机 E2E 与证据归档
```

每个 Story 必须含：目标、依赖、允许路径、禁止路径、验收标准、验证命令、证据位置、回滚点。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：按“业务能力 + 独立 oracle”拆解，而不是按文件数量或开发角色拆。
- 怎么判断：一个 Story 完成后，即使后续 Story 尚未实现，也能得到明确 PASS/FAIL/UNKNOWN。
- 不对怎么办：如果 Story 需要一次打开二三十个无关文件，或只能在整个 Feature 完成后才能判断，继续拆分或先做技术穿刺。
- 本页产出：Feature decomposition 与 Story 依赖图。

### 讲师备注

- 解释为什么 S1/S2 在前：如果账号真相和时序没证明，后面的策略代码写得再快也可能建立在错误输入上。
- 这也是 Ralph 一次只做一个小任务的前提：小任务必须有可判断的结束条件。

---

## 第 14 页｜结构化目录是长任务的外部记忆，也是不同 Session 的共同协议

### PPT 内容

展示真实 Harness 目录，而不是抽象讲“上下文工程”：

```text
AGENTS.md
docs/03-模块设计/
docs/ai-delivery/
├── DELIVERY-RULES.md
├── harness.config.json
├── test-id-contract.json
├── features/<feature-id>.md
├── stories/<story-id>.md
└── templates/
    ├── FEATURE-DECOMPOSITION.md
    ├── STORY.md
    ├── WORKER-PACKET.md
    └── ACCEPTANCE-REPORT.json
scripts/ai-delivery/
└── run_stage.py
.ai-delivery/results/<run-id>/
├── report.json
├── report.md
└── stdout-stderr/
```

顶部结论：**上下文不只放在聊天记录里；真正需要跨 Session 保留的事实，必须落到仓库。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：规则放 `AGENTS.md`，设计放模块文档，任务放 Story，执行结果放 evidence，进度只记录事实与下一步。
- 怎么判断：新 Session 不读历史对话，只读这些文件，也能知道目标、边界、当前状态和验证方式。
- 不对怎么办：如果换会话后必须重新口述背景，说明上下文没有结构化；先补交接包，不继续堆聊天长度。
- 本页产出：项目级外部记忆规范。

### 讲师备注

- 现场打开真实目录，按“规则—设计—任务—证据”四层浏览，不要逐文件解释。
- 强调目录本身不是价值，价值是每种事实只有固定归宿，AI 不需要在 400 次历史 Session 中寻找真相。

### 演示 / 素材

- `CURRENT`：使用 SecurityTool 的真实目录截图。
- 本地依据：`C:/Users/mu/Documents/Codex/2026-08-22/c-users-mu-desktop-code-security/outputs/HarmonyOS-AI研发闭环从零搭建指南.md`。

---

## 第 15 页｜“先改文档再干活”不是口号，而是让行为变化先留下可审查契约

### PPT 内容

用真实 Git 时间线呈现：

```text
21:58:55  4b372d0d  docs: 定义账号新增的稳定快照同步语义
22:03:53  c0c1bc9f  code/test: 实现等待、重试与验证
```

文档先行的最小流程：

1. 阅读对应模块设计。
2. 标出行为、状态、权限、错误或验收口径是否变化。
3. 先更新模块设计和 Story AC。
4. 评审“设计是否说得通”。
5. AI 才能改代码、测试并回写证据。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：每次非平凡修改都在 Story 中引用对应设计章节和 AC ID。
- 怎么判断：Git Diff 中应先看见“行为契约改变”，再看见实现；代码和测试能逐条映射回文档。
- 不对怎么办：AI 已经改了代码但无法解释对应设计变化时，暂停，不让它补一段事后合理化文档；先用现有行为和需求重新评审。
- 本页产出：可审查的设计变更与提交边界。

### 讲师备注

- 这页要用真实提交记录，而不是做一张漂亮的假 Git 图。
- 说明文档先行不意味着瀑布式冻结一切；它意味着在模型开始大量执行前，先把本轮要改变的规则说清楚。

### 演示 / 素材

- `CURRENT`：提交详情截图，左右并排展示模块设计 Diff 与代码/测试 Diff。
- 本地依据：SecurityTool Git 提交 `4b372d0d`、`c0c1bc9f`。

---

## 第 16 页｜给 AI 的不是一句“实现功能”，而是一份可执行 Worker Packet

### PPT 内容

画面中间放一张真实风格任务卡：

```yaml
story: FW-ACCOUNT-ADD-STABLE
goal: 新增账号后，仅在稳定账号快照包含该账号时触发同步
allowed_paths:
  - AccountChangeCoordinator.ets
  - account-change-coordinator.test.ets
forbidden:
  - FirewallPage.ets
  - shared AVC / unrelated modules
acceptance:
  - first old snapshot must not dispatch
  - later snapshot containing id dispatches once
  - timeout preserves previous state
verify:
  - unit test
  - docs consistency
  - build / device story profile
```

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：Planner 只负责产生任务包；Implementer 按包工作；Reviewer 只拿规格、Diff 和证据评审。
- 怎么判断：AI 不需要猜范围、猜完成条件，也不能用自己新增的测试作为唯一依据。
- 不对怎么办：发现需要越过 `forbidden` 或新增跨模块依赖时，停在 `NEEDS_REPLAN`，由人/Planner 更新 RFC 和任务包。
- 本页产出：可直接交给新 Session 执行的 Worker Packet。

### 讲师备注

- 对比两种提示：“修复账号同步问题”与这张任务卡。让学员说出后者减少了哪些自由度。
- 强调限制路径不是僵化。如果真正的根因在范围外，正确动作是重规划，而不是偷偷扩散修改。

---

## 第 17 页｜遇到系统能力不确定时，先做“最小能力穿刺”，不要直接建设完整业务

### PPT 内容

本 Story 的未知项不是“怎么写循环”，而是：**HarmonyOS 账号事件与账号列表在时间上是否一致？**

穿刺只回答四个问题：

1. 能否从系统 API 读取真实账号 ID 列表？
2. 新增账号事件里得到的 `accountId` 是什么？
3. 同一时刻再次读取列表，是否已经包含这个 ID？
4. 需要哪些权限、管理员状态和设备条件？

穿刺产物不是正式 Feature，而是一个最小调用链、真实日志和平台结论。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：调用 `osAccount.getAccountManager().getOsAccountLocalIds()`；同一时间轴记录事件 ID、列表、时间和设备动作。
- 怎么判断：以系统 getter 返回的集合为真相源，以事件为触发信号，不用 UI 文案或缓存证明账号存在。
- 不对怎么办：API 不可用先查权限/管理员/设备版本；API 可用但数据不同步则记录时序，不先写补丁掩盖。
- 本页产出：技术可行性结论和未知项清单。

### 讲师备注

- “穿刺”要讲成风险前置：只打通最短路径，尽快知道关键假设是否成立。
- 可以现场让 AI 先列验证命令和预期日志，不让它直接修改 Coordinator。

### 演示 / 素材

- `PLACEHOLDER`：真机新增账号操作 10 秒视频。
- `CURRENT`：`entry/src/main/ets/services/firewall/providers/SystemUserProvider.ets`。

---

## 第 18 页｜真实问题：账号新增事件先到了，但系统账号列表仍是旧快照

### PPT 内容

用真实时间线替代理论描述：

```text
T0  onAccountAdded(accountId=123)
T0  loadAvailableUserIds() → [100, 112, 122]
T0  旧实现：认为集合未变化，跳过同步
T1  系统列表稍后才出现 123
结果：账号 123 没有获得当前防火墙模式与规则
```

顶部结论：**事件是“发生了变化”的通知，不一定是“变化已可见”的事实。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：把回调参数、provider 查询结果、持久化签名和最终策略同时放进一个时间轴。
- 怎么判断：如果事件 ID 已出现而真相源集合未包含它，就是跨进程可见性窗口，不是业务没有变化。
- 不对怎么办：不要让 UI 强制刷新、不要直接把事件 ID 塞进集合；先定义何时快照才可信。
- 本页产出：根因假设——“事件/真相源的可见性延迟”。

### 讲师备注

- 这页应该用真实日志截图；如果暂时没有，就保持日志占位，不要造一段看似真实的控制台。
- 问学员：“直接把 123 append 到列表行不行？”引出下一页：删除、重命名、读取失败和多事件并发会让这种补丁不可靠。

### 演示 / 素材

- `PLACEHOLDER`：真实 Session 日志，红框标出事件 `123` 与旧集合不一致。
- 本地依据：`docs/superpowers/plans/2026-07-02-firewall-account-added-stable-snapshot.md`。

---

## 第 19 页｜判断方案是否正确，要先决定“谁是真相、何时可信、失败时保持什么”

### PPT 内容

将三个方案放在对比表：

| 方案 | 优点 | 失败模式 | 结论 |
|---|---|---|---|
| 直接信事件 ID | 快 | 无法代表完整最新集合 | 否 |
| 读取一次账号列表 | 简单 | 可能读到旧快照 | 否 |
| 等待列表包含触发 ID | 以系统集合为真相，可验证 | 需要重试上限和超时语义 | 是 |

冻结业务契约：

- 新增事件：等待账号列表包含触发 ID。
- 400ms debounce，最多 5 次、每次间隔 200ms（当前案例配置）。
- 超时：不分发、不发布、不更新 previous state，保留旧的可信状态。
- 删除/手工同步：仍只读一次，不套用新增语义。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：把重试条件和超时结果写进 RFC/Story，而不是散落在代码常量里。
- 怎么判断：等待的是业务条件“集合包含 ID”，不是单纯 sleep 固定时长。
- 不对怎么办：超时不制造成功；记录 `UNKNOWN/INCOMPLETE` 和最后观察到的集合，交给后续诊断。
- 本页产出：稳定快照契约。

### 讲师备注

- 强调数字不是通用最佳实践，而是这个项目的当前策略；真正可迁移的是“按业务条件重试 + 上限 + 保持旧真相”。
- 让学员判断：为什么删除事件不等待“列表不含 ID”？可以讨论是否未来需要对称设计，但本 Story 不擅自扩大范围。

---

## 第 20 页｜先写能反证旧实现的测试，再让 AI 写最小实现

### PPT 内容

展示 RED 测试的行为，而不是贴大量代码：

```text
第 1 次 query → [100, 122]
期望：不 dispatch

第 2 次 query → [100, 122, 123]
期望：只 dispatch 一次，输入为稳定集合

5 次仍没有 123
期望：不 dispatch、不 publish、不更新 previous state
```

右下角写：**测试的价值不是覆盖行数，而是让错误实现无法伪装成正确。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先让测试在旧实现上失败并保存 RED 日志，再实现，再保存 GREEN 日志。
- 怎么判断：测试输入包含“先旧后新”和“持续旧”两个时序；仅测试最终新集合不够。
- 不对怎么办：测试一开始就绿，检查是否真正穿过旧错误路径，不能直接接受。
- 本页产出：独立于新实现的时序 oracle。

### 讲师备注

- 如果现场时间允许，可以把第二次查询改成第三次，让学员预测 dispatch 次数。
- 指出 mock 只能证明 Coordinator 的协议；真机仍要证明平台确实存在这种时序和最终状态。

### 演示 / 素材

- `CURRENT`：`entry/src/test/firewall/account-change-coordinator.test.ets`。
- `PLACEHOLDER`：RED/GREEN 测试输出截图。

---

## 第 21 页｜最小修复只改变稳定快照协调器，不用 UI 补丁掩盖状态问题

### PPT 内容

实现逻辑用 8 行伪代码呈现：

```text
onAccountAdded(id):
  debounce()
  repeat up to 5:
    ids = provider.loadAvailableUserIds()
    if ids contains id:
      dispatchStableSnapshot(ids)
      return PASS
    wait 200ms
  return INCOMPLETE  // preserve previous truth
```

旁边放范围控制：

- 改：`AccountChangeCoordinator` 与对应测试。
- 不改：页面生命周期、UI 强刷、其他模式 Handler、无关模块。
- 保留：删除/手工同步原有单次读取语义。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：按 Worker Packet 只修改最小职责层。
- 怎么判断：Diff 小且能完整满足 P19 契约；失败分支不会改变已有可信状态。
- 不对怎么办：如果必须改 UI 才能让测试通过，说明修复层级错误；回到真相源和状态流重新检查。
- 本页产出：最小代码 Diff 与范围审计。

### 讲师备注

- 可在真实 Diff 上标三种颜色：新增等待逻辑、保持的旧路径、测试断言。
- 这里回扣“AI 几乎正确”：多写一个页面 reload 可能让演示看起来正常，却没有修复后台同步。

---

## 第 22 页｜每个 Story 都必须独立过门，Feature 通过不能替代 Story 证据

### PPT 内容

Story 验收表：

| 验收层 | 本 Story 证据 | 状态 |
|---|---|---|
| 规格 | AC 与稳定快照契约一致 | PASS / FAIL |
| 单测 | 先旧后新、超时、单次 dispatch | PASS / FAIL |
| 静态/文档 | 路由、权限、测试 ID、UTF-8 一致性 | PASS / FAIL |
| 构建 | `entry@default` HAP | PASS / FAIL |
| 环境 | 签名、安装、企业管理员 | PASS / FAIL / UNKNOWN |
| 业务 | 新账号最终获得当前模式/规则 | PASS / FAIL / UNKNOWN |

底部强调：报告必须同时记录命令、退出码、证据路径和未执行原因。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：Story 完成即运行 `Story profile`，不是等整个 Feature 最后一次“大验收”。
- 怎么判断：所有必需 AC 都有证据映射；缺真机就明确 UNKNOWN。
- 不对怎么办：某一门失败只回到对应责任层，避免“全仓重写式修复”。
- 本页产出：`acceptance-report.json` + 人可读报告。

### 讲师备注

- 这里开始引入结构化验收，为下一页 MCP 做铺垫。
- 可展示真实报告 JSON 的局部：`storyId`、`status`、`checks[]`、`evidence[]`、`unknowns[]`。

---

## 第 23 页｜MCP 的价值是把验证接入真实环境，不是给 AI 多一个“成功”按钮

### PPT 内容

把一次 MCP 调用拆成四层结果：

```text
Protocol：请求有没有送达、格式是否正确
Tool：构建/安装/点击/读取是否执行成功
Business：应用状态是否按需求变化
System：系统 getter、权限、策略是否真实生效
```

举例：`install_app = success` 只证明工具层安装成功；它不能证明企业管理员已激活，更不能证明防火墙策略已经应用到新账号。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：MCP 负责固定的 Build → Sign → Install → Operate → Observe；业务验收仍读取日志、getter、UI 和持久化状态。
- 怎么判断：每个工具结果必须绑定“它能证明什么/不能证明什么”。
- 不对怎么办：工具 success 但业务无变化时，不重复点按钮；转向业务层和系统层证据。
- 本页产出：工具契约与证据映射表。

### 讲师备注

- 可以现场让学员分类：截图、测试报告、安装返回码、系统账号列表分别属于哪一层。
- 强调 MCP 是 Verify 的执行接口，不是第三套开发方法论。

### 演示 / 素材

- `PLACEHOLDER`：MCP 构建、安装、设备操作、日志读取四步截图。
- 本地依据：SecurityTool `.ai-delivery/results/` 结构和 `scripts/ai-delivery/run_project_e2e.py`。

---

## 第 24 页｜真实验收曾经在最后一米失败：环境全绿，E2E 因测试契约缺失而失败

### PPT 内容

展示真实 smoke 结果：

| 检查 | 结果 |
|---|---|
| 文档一致性 | PASS |
| 保护资产 | PASS |
| HAP 构建 | PASS |
| 设备连接 | PASS |
| 安装 | PASS |
| 企业管理员激活 | PASS |
| 导航 E2E | **FAIL：缺少 `sidebar-nav-firewall` 测试 ID** |

页面结论：**前六项成功没有抵消第七项失败；Harness 的价值正是阻止“看起来差不多”被写成 PASS。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：测试 ID 作为 UI 与自动化之间的显式契约，放进 `test-id-contract.json` 并做静态检查。
- 怎么判断：自动化找不到元素时，先区分“产品不可用”“测试契约缺失”“工具定位失败”。
- 不对怎么办：保持报告 FAIL；补契约或修定位后重跑，不用人工点击成功覆盖机器失败。
- 本页产出：一个真实的“AI 完成 ≠ 交付”证据卡。

### 讲师备注

- 这是案例一最重要的反例之一。讲师要明确：失败报告不是流程拖慢，而是把发布前的未知变成可修复事实。
- 如果有真实报告截图，放大 FAIL 行，其他 PASS 缩小；不要重造日志。

### 演示 / 素材

- `PLACEHOLDER`：真实 `report.md` / E2E 失败截图。
- 本地依据：`C:/Users/mu/Documents/Codex/2026-08-22/c-users-mu-desktop-code-security/outputs/AI如何完成鸿蒙应用开发闭环-SecurityTool实践分享.md`。

---

## 第 25 页｜交流练习：证据互相矛盾时，你会给 PASS、FAIL 还是 UNKNOWN？

### PPT 内容

给学员一张混合证据卡：

```text
单元测试：PASS
HAP 构建：PASS
安装与管理员激活：PASS
UI：新账号页面已出现
Coordinator 日志：重试 5 次仍未看到 accountId=123
系统策略 getter：本次未采集
```

要求小组 3 分钟回答：

1. 当前状态是 PASS、FAIL 还是 UNKNOWN？
2. 哪条证据权重最高？
3. 下一步只允许做一个动作，做什么？
4. 结果要写回 PRD、RFC、Story 还是 acceptance？

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：小组先给状态，再按“最强证据—缺失证据—下一动作—写回位置”说明理由。
- 不能给 PASS：业务真相缺失，且协调器没有获得稳定快照。
- 若需求要求本轮必须完成新账号策略同步，则是 FAIL；若设备 getter 暂不可用、还不能确认最终系统状态，可明确为 UNKNOWN，但必须说明阻塞证据。
- 不对怎么办：优先采集系统账号/策略真相与最后稳定快照；不是继续修改 UI。
- 写回：本轮 acceptance；若发现原契约不充分，再更新 RFC/Story。

### 讲师备注

- 先投票再公布参考答案。允许学员争论 FAIL/UNKNOWN，但必须用“需求是否已经明确违反”和“证据是否缺失”解释。
- 收束案例一：原始图片不是直接变代码，而是沿着 PRD → RFC → Story → 实现 → MCP/真机证据逐层缩小不确定性。
- 转场：第二个案例没有一张清晰需求图，只有“CPU 高、视频卡”。因此入口要从 Evidence-first 开始。

---

## 第 26 页｜案例二不是从零写应用，而是在大型开源系统里替换一条关键链路

### PPT 内容

`TEACHING SKELETON`：本页只定义案例进入方式，具体仓库、代码量、模块结构和界面由负责该案例的 Session 回填。

建议背景信息按四句话讲清：

1. 原应用解决什么远控场景。
2. 依赖的开源库规模与主要平台边界是什么。
3. 当前链路为什么主要消耗 CPU。
4. 这次需求只准备替换哪一段能力，不准备重写什么。

建议用项目真实结构回填一条端到端链路：

```text
远端数据 → 协议/解码入口 → CPU 或硬件路径 → 合成 → 页面输出
```

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先把“远控很卡”翻译成明确的改造目标、非目标、影响链路和可测指标。
- 怎么判断：学员能指出需求要改变的链路位置，以及哪些原能力必须保留。
- 不对怎么办：如果问题范围仍是“很卡”，先建立基线和帧链，不让 AI 全仓搜索“性能优化”。
- 本页产出：系统边界与改造目标。

### 讲师备注

- 这里讲思想，不讲尚未核验的项目事实。具体数字必须来自仓库统计并注明统计口径和日期。
- 复杂度重点不是“代码多”，而是陌生代码、跨平台行为、性能链路和旧能力不能被破坏。

### 演示 / 素材

- `PLACEHOLDER`：真实仓库统计、模块树、应用界面和端到端链路图，由案例 Session 回填。

---

## 第 27 页｜症状是 CPU 高和视频卡，但根因可能在解码、队列、合成或输出所有权

### PPT 内容

左侧只放经过确认的用户现场：

- CPU 占用高。
- 远控中的视频播放明显卡顿。
- `PLACEHOLDER`：其他现象必须从真实视频、日志或 Session 中回填，暂不推断。

右侧放假设树：

```text
没有进入硬解？
硬解成功但 NativeBuffer 导入失败？
中间结果成功但没有在正确生命周期提交？
新路径已输出却被旧路径覆盖？
队列丢了关键提交事件 / 追踪 ID 不匹配？
视频区域正确，其他区域没有可信背景？
```

顶部结论：**用户看到的是最终像素，不能从症状直接跳到某一层修代码。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先定义一次可重复的基线场景，采集 CPU、帧率、时延、内存以及对应视频；技术计数项由真实链路决定。
- 怎么判断：每个假设都必须对应一个可观察信号和一个最小实验。
- 不对怎么办：没有基线就不接受“优化后更流畅”；日志只证明中间步骤时，继续追到最终像素。
- 本页产出：症状—假设—证据矩阵。

### 讲师备注

- 这一页一定预留真实视频。没有视频时只显示占位框，不用互联网视频代替项目现场。
- 告诉学员，后面不会按“AI 尝试了很多方案”的流水账讲，而是用证据逐层砍掉错误假设。

### 演示 / 素材

- `PLACEHOLDER`：CPU 曲线 + 卡顿视频 + 绿/黑帧截图。
- `PLACEHOLDER`：问题视频、性能基线、用户原话和首轮日志，由案例 Session 回填。

---

## 第 28 页｜让 AI 快速熟悉大仓，第一步不是“读完仓库”，而是建立可路由的代码地图

### PPT 内容

四层勘察法：

1. **规则层**：`AGENTS.md`、构建说明、模块文档、已有复盘。
2. **入口层**：应用入口、Native/ArkTS 边界、协议回调注册点。
3. **链路层**：只追一条目标调用链——输入事件 → 解码 → 缓冲区 → 合成 → 提交。
4. **对标层**：找到其他平台处理同一协议事件的实现，不横向阅读所有文件。

勘察输出只保留：符号、文件、职责、调用方向、未证实假设和下一步读取条件。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：先 `rg` 符号和回调注册；按调用链逐文件展开；大日志只截取 frameId/状态变化相关窗口。
- 怎么判断：AI 能画出 10–15 个节点的链路，并指出每个节点的输入、输出和所有者；不要求记住全仓。
- 不对怎么办：上下文出现大量无关文件或重复搜索时，停下生成 `evidence-index.md`，新会话只加载任务包和索引。
- 本页产出：Code Map + Evidence Index。

### 讲师备注

- 现场展示“错误做法：把整个三方库塞进上下文”与“正确做法：按符号路由”的对比。
- 可引用 Claude Code 上下文文档的原则：文件和工具输出都会占用上下文，长会话性能会下降；子任务/新上下文适合隔离探索。

### 演示 / 素材

- `PLACEHOLDER`：一次真实 `rg` 结果到 6 个关键文件的收敛过程。
- `[Sources]` https://code.claude.com/docs/en/context-window  
  `[Sources]` https://code.claude.com/docs/en/best-practices

---

## 第 29 页｜上下文工程的目标不是压缩所有信息，而是让当前 Story 只看到做决定所需的证据

### PPT 内容

给 AI 的上下文包：

```text
任务：验证一个最小的硬件解码/显示链路
必须读：集成 RFC、目标调用链、对标实现、最近一轮真实日志
允许改：本次穿刺涉及的适配层、专用测试与诊断日志
禁止改：共享协议层、无关显示路径、UI 布局及未纳入本 Story 的模块
已知事实：从基线与代码勘察中回填
未知：从平台调研和首次穿刺中回填
完成：最小输入能走完整目标链路，失败时原路径仍可用
```

页面右侧列出“不要放入上下文”的内容：整个历史聊天、全量构建日志、所有平台代码、已经被证伪的长方案。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：每个 Story 生成一页 Context Manifest；原始证据在文件系统，Prompt 只引用路径和结论。
- 怎么判断：新 Session 在 5 分钟内能复述边界、关键链路、当前未知和完成条件。
- 不对怎么办：AI 重新提出已证伪假设，说明交接包没记录决策；把“假设—证据—结论”写回，而不是继续口头纠正。
- 本页产出：可复用上下文包。

### 讲师备注

- 这一页回扣 Ralph 的 fresh context：新上下文不是失忆，而是把记忆从聊天迁到结构化文件。
- 禁止路径要来自仓库边界和影响面分析；具体发生过的范围污染案例由案例 Session 提供后再写入。

---

## 第 30 页｜先研究其他平台如何维持协议语义，再决定 HarmonyOS 要替换哪一层

### PPT 内容

`TEACHING TEMPLATE`：选择该开源库已经支持的 2–3 个平台，对照同一个输入事件如何走到最终输出。

```text
同一协议/媒体输入
→ 平台 A 的解码、缓存、合成、提交
→ 平台 B 的解码、缓存、合成、提交
→ 提取共同语义与平台差异
```

研究目标不是照抄 API，而是提取不变量：

- 谁拥有最终输出。
- 哪个事件代表一帧或一次事务结束。
- 局部更新依赖什么历史状态。
- 新路径失败时如何保留旧路径。
- 哪些是协议语义，哪些只是平台 API 选择。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：让 AI 从其他平台各选择一条最短实现链路，做“同一输入—状态变化—最终输出”的对照表。
- 怎么判断：输出的是跨平台不变量和 HarmonyOS 差异，不是复制函数列表。
- 不对怎么办：如果对标结论无法解释当前现象或关键生命周期，继续回到协议语义，不急着选平台 API。
- 本页产出：Reference Behavior Matrix。

### 讲师备注

- 重点说“了解其他平台”不是搜一篇博客，而是从上游实现抽取稳定语义。
- HarmonyOS 可以使用不同 API，但对接方案必须说明哪些跨平台行为不能改变。

### 演示 / 素材

- `PLACEHOLDER`：2–3 个平台的真实代码入口、调用链与对照结论，由案例 Session 回填。

---

## 第 31 页｜平台调研要把“API 可用”变成一张可落地的能力与风险表

### PPT 内容

HarmonyOS 对接能力表（字段模板，具体 API 与结论待回填）：

| 阶段 | 候选能力 | 必须验证 | 常见风险 |
|---|---|---|---|
| 解码 | 官方候选接口 | 输入格式、回调、连续帧状态 | 配置成功不等于正确输出 |
| 缓冲区 | 平台缓冲能力 | 格式、步长、生命周期、同步 | 隐式拷贝抵消收益 |
| 图形导入 | 平台图形接口 | 扩展、纹理、色彩与错误码 | 导入成功但像素错误 |
| 合成 | GPU 合成能力 | 多层、局部更新、历史状态 | 新旧内容不一致 |
| 显示 | 页面/窗口输出能力 | 线程、生命周期、提交时机 | 过早、过晚或被旧路径覆盖 |

顶部结论：**调研完成的标志不是找到文档，而是每个能力都有最小验证和失败回退。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：每项输出 `Supported / Unsupported / Unknown`，并附运行证据或最小实验计划。
- 怎么判断：能区分“接口存在”“调用成功”“得到正确像素”“端到端性能改善”四层事实。
- 不对怎么办：平台能力仍 Unknown 时保持 CPU fallback，不把未知接口写入完整架构承诺。
- 本页产出：Platform Capability Matrix。

### 讲师备注

- 这页讲调研深度：API 名称只解决“可能怎么做”，运行证据才解决“在本设备/版本能否做”。
- 具体 API 名称、支持版本和设备结果全部由案例 Session 依据官方文档与真机结果回填。截图只证明 API 契约，运行证据才证明当前环境行为。

### 演示 / 素材

- `PLACEHOLDER`：官方 API 文档、设备能力日志和最小示例运行结果。

---

## 第 32 页｜对接 RFC 必须同时设计新 GPU 路径、旧 CPU 回退和唯一输出所有者

### PPT 内容

`TEACHING SKELETON`：用候选架构图表达新旧路径如何共存，节点名称按真实代码回填。

```text
输入事件
 ├─ 新硬件路径：decode → buffer → compose → candidate result
 └─ 原有 CPU 路径：保持可回退

统一提交点
 ├─ 新路径完整消费：由新路径输出
 └─ 新路径未完成：由原路径输出
```

RFC 冻结四条规则：

1. 验证通过前，GPU 不接管输出所有权。
2. 同一个输入只在新路径完整消费后抑制旧路径。
3. 提交时机对齐协议或渲染生命周期，不在任意中间回调提前结束。
4. 失败路径保留 CPU/GDI 可见结果。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：显式建模“候选结果、是否完整消费、输出所有者、是否可提交”，具体状态名以实际实现为准。
- 怎么判断：任意一次输出都能回答“谁处理、谁持有最终结果、为什么此刻可以提交”。
- 不对怎么办：最终结果异常时先沿状态与提交时序定位，不从症状直接猜某个 API。
- 本页产出：Integration RFC 与状态机。

### 讲师备注

- 这是案例二的设计中心。要把“硬解成功”和“可以接管画面”明确分开。
- 这页讲设计检查项，不宣称当前项目已经采用此状态模型；案例 Session 应用真实代码验证或修订它。

### 演示 / 素材

- 建议自绘双路径/所有权状态图。
- `PLACEHOLDER`：最终 RFC 状态图和实际类/函数映射。

---

## 第 33 页｜最小能力穿刺只证明一帧闭环，不先替换整个远控输出

### PPT 内容

穿刺目标分五级，按顺序过门：

```text
L1 收到一个可识别的目标输入与追踪 ID
L2 平台解码能力产生有效输出
L3 输出成功进入图形/显示链路
L4 单帧或最小区域正确显示
L5 在正确生命周期提交，且失败时原路径仍可见
```

本轮明确不做：完整功能迁移、全路径性能优化、删除原路径，以及尚未被穿刺证明的扩展目标。具体非目标由案例 Session 回填。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：用固定测试素材/远端场景，只打通一帧；每一级记录输入、输出、frameId 和结果。
- 怎么判断：L1–L5 逐级可证；FPS 提升不是本轮唯一成功条件，画面/所有权/回退先成立。
- 不对怎么办：在哪一级失败就停在哪一级；例如图形导入失败不去改最终提交，单帧结果错误不先做吞吐优化。
- 本页产出：Spike Report 与 Go/No-Go 结论。

### 讲师备注

- 对比常见错误：一上来把完整 GPU compositor 接入所有路径，失败后不知道是 codec、buffer、shader 还是 present。
- “最小”不是 demo 随便跑通；它仍需包含回退，因为不破坏现有能力是本次可行性的一部分。

### 演示 / 素材

- `PLACEHOLDER`：最小输入、各阶段 Trace、正确画面和失败回退结果。

---

## 第 34 页｜穿刺通过后再拆 Story，每个阶段都有独立验收点和禁止扩散范围

### PPT 内容

Story / 验收矩阵：

| Story | 范围 | 独立验收 |
|---|---|---|
| G1 Decode Adapter | 输入到平台解码输出 | 连续样本可处理、错误可观测 |
| G2 Buffer / Import | 解码输出到图形对象 | 格式、步长、生命周期正确 |
| G3 Compose | 新内容与原有背景合成 | 局部更新不破坏其他区域 |
| G4 Output Ownership | 新旧路径协调 | 不覆盖、不双重提交 |
| G5 Commit Timing | 队列与提交点 | 追踪 ID 对齐、关键事件不丢失 |
| G6 Fallback & Recovery | 新路径失败切回原路径 | 故障注入后仍可见、可恢复 |
| G7 Performance Evidence | CPU/FPS/内存/时延 | 同场景前后对比、无功能回归 |

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：Story 依赖按链路排列；每个 Worker Packet 标出允许文件和 protected shared path。
- 怎么判断：每个 Story 的 oracle 不依赖后续 Story；性能结论必须在功能正确后测量。
- 不对怎么办：一个改动同时碰解码、队列、合成、UI 时退回重拆；共享协议或编解码路径变化必须单独评审。
- 本页产出：GPU Feature decomposition。

### 讲师备注

- 让学员看到方法与案例一相同：只有入口从 PRD-first 变成 Evidence/Spike-first，穿刺完成后仍回到 Story 与验收。
- 这些 Story 名称是方法模板，另一个 Session 应按真实模块边界调整，不能机械套用。
- 明确 “G7 性能”放最后不是不重视性能，而是错误画面下的高 FPS 没有价值。

---

## 第 35 页｜真实开发不是直线：AI 多次“几乎正确”，证据迫使它退回正确层

### PPT 内容

本页只保留错误复盘结构；具体事件必须从真实 Session 回填：

1. **错误假设**：AI 为什么会这样判断。
2. **最小改动**：它具体改了哪一层，是否越过任务边界。
3. **反证**：哪条日志、画面、性能或回归证明它不成立。
4. **处置**：回退什么、保留什么、下一轮缩小到哪里。

建议至少回填三类真实问题：范围污染、局部成功但端到端失败、性能改善却引入功能或资源回归。

页面结论：**AI 的错误不可怕；可怕的是没有保存反证，下一轮又从同一假设开始。**

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：每次失败写一张 Decision Record：假设、最小修改、观察、反证、回退、禁止重试条件。
- 怎么判断：后续 Session 能明确说出哪些方案已被什么证据否定。
- 不对怎么办：范围污染时先恢复受影响路径并做回归，再继续主任务；不能把“顺手修好”当合理扩展。
- 本页产出：Failed Attempts Ledger。

### 讲师备注

- 这页最终必须使用真实 Session 话语和 Diff/日志，体现复杂需求中人的判断责任；目前不预写具体结论。
- 不要把失败讲成“模型笨”；要讲清楚当时为什么看似合理、哪条证据使它失效、工程系统如何阻止错误继续扩散。

### 演示 / 素材

- `PLACEHOLDER`：用户纠偏原话、错误 Diff、反证日志、回退记录和问题画面，由案例 Session 回填。

---

## 第 36 页｜判断 AI 对不对，要用一条帧级证据链把中间成功连接到最终像素

### PPT 内容

画一条带相同追踪 ID 的 Trace；具体事件名由真实实现回填：

```text
input received
→ decode started / output ready
→ buffer imported
→ content composed
→ output owner selected
→ matching commit point
→ final output
→ screenshot / video / user-visible result
```

证据分三类：

- **Trace**：追踪 ID、对象 ID、时间、路径与状态迁移。
- **Counters**：进入、成功、失败、回退、错配和丢弃；具体计数器由案例实现决定。
- **Outcome**：像素采样、截图/视频、FPS、CPU、内存和用户体验。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：同一帧贯穿所有阶段；日志只记录状态变化和异常，不刷屏。
- 怎么判断：Trace 解释“怎么走到结果”，Outcome 证明“结果是否正确”，两者缺一不可。
- 不对怎么办：中间计数绿但最终结果错误时，从最终输出反向定位第一处错误状态；不要先改 UI 或颜色掩盖。
- 本页产出：Frame Evidence Contract。

### 讲师备注

- 这一页只定义“过程 Trace + 最终 Outcome”的双证据思想。具体白帧、绿帧、旧帧或队列问题，选一个证据最完整的真实案例回填。
- 回填时必须说明：最初假设、最终结果位于哪一层、第一处错误事实在哪里、为什么最终修复有效。

### 演示 / 素材

- `PLACEHOLDER`：一条真实追踪日志、对应截图/视频和反向定位过程。

---

## 第 37 页｜结果页只报告有证据的改善，同时保留尚未证明的边界

### PPT 内容

结果页仍分三栏，但所有项目事实与数字都留待案例 Session 回填：

**已证明**

- `PLACEHOLDER`：完成了哪一条端到端能力。
- `PLACEHOLDER`：同场景功能、CPU、FPS、内存或时延对比。
- `PLACEHOLDER`：自动化、真机、视频和日志证据路径。

**由方案保证**

- 哪些系统不变量已经由设计和测试保证。
- 新路径失败时如何回退。
- 输出所有权和提交时机如何保持一致。
- 哪些回归场景已经执行。

**仍需验证**

- 未覆盖的设备、分辨率、网络和输入组合。
- 长时间运行的资源、恢复与稳定性。
- 尚未纳入本轮的共享路径和功能回归。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：结果卡同时写测试场景、版本、设备、指标、证据路径和限制。
- 怎么判断：不把单次运行写成普遍性能结论；可复现、可回放、可解释才进入“已证明”。
- 不对怎么办：指标改善但画面/回退失败，整体仍 FAIL；未覆盖场景进入 UNKNOWN/后续 Story。
- 本页产出：有边界的 Outcome Report。

### 讲师备注

- 这页要训练“克制表达”。AI 特别容易把局部成功总结成项目完成，讲师要示范如何限定证据范围。
- 最后总结案例二的收获：大仓不是靠读完；跨平台不是照抄；中间步骤成功不是最终正确；工程能力体现在范围、回退、观测和验收。

### 演示 / 素材

- `PLACEHOLDER`：同一场景 CPU/FPS/内存/时延前后对比，以及最终视频；必须标设备与采集条件。

---

### 案例二 Session 回填契约（不占 PPT 页数）

负责具体案例的 Session 只需要沿现有 12 页骨架回填，不必重做叙事结构：

| 页码 | 必须回填的真实内容 | 最低证据 |
|---:|---|---|
| 26 | 项目背景、仓库规模、真实链路、改造目标与非目标 | 仓库统计、目录或架构图 |
| 27 | CPU 高、视频卡顿及其他实际症状 | 同场景视频、性能基线、用户原话 |
| 28 | AI 如何从大仓收敛到关键代码 | 搜索过程、Code Map、关键入口 |
| 29 | 当前 Story 的上下文包和禁止范围 | Context Manifest、允许/禁止路径 |
| 30 | 其他平台如何实现同一协议/媒体行为 | 2–3 条真实上游调用链 |
| 31 | HarmonyOS 平台能力与限制 | 官方文档、最小示例、真机日志 |
| 32 | 最终对接 RFC 与新旧路径状态机 | RFC、实际类/函数映射 |
| 33 | 最小能力穿刺 | 最小输入、逐段 Trace、正确结果与回退 |
| 34 | 穿刺后的 Story 拆解 | Story、依赖、AC、验证命令 |
| 35 | 真实错误尝试与用户/证据纠偏 | Session 原话、错误 Diff、反证、回退 |
| 36 | 判断 AI 是否正确的端到端证据链 | 同一追踪 ID 的 Trace + 最终视频/截图 |
| 37 | 已证明结果、设计保证、剩余未知 | 前后对比、测试条件、证据路径与限制 |

回填原则：没有证据的内容继续保持 `PLACEHOLDER`；单次结果必须写清测试条件；任何具体 API、函数、数字和结论都应能回到代码、官方文档、Session、日志或视频。

---

## 第 38 页｜两个案例的入口不同，但最终都收敛到同一套复杂需求作战图

### PPT 内容

整页主视觉：

```text
1 明确目标与非目标
        ↓
2 找到业务/系统/协议真相源
        ↓
3 记录未知项与错误成本
        ↓
4 最小能力穿刺，先证关键假设
        ↓
5 PRD / RFC / Story 固化边界与验收
        ↓
6 AI 在受控 Loop 中一次完成一个 Story
        ↓
7 测试、构建、MCP、真机与 Outcome 分层验收
        ↓
8 把结果、失败和下一步写回文档
```

左右标注差异：

- MDM：需求先行，穿刺验证系统时序。
- 远控：证据先行，穿刺验证平台链路。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：每到新项目，先判断应从“需求不确定”还是“技术事实不确定”进入，但不跳过后续闭环。
- 怎么判断：任何 Story 都能追溯到需求/假设，任何完成都能追溯到独立证据。
- 不对怎么办：需求错退 PRD，设计错退 RFC，输入假设错退 Spike，实现错退 Story，环境错退验证层；不要让所有错误都表现为“继续改代码”。
- 本页产出：一张可带走的复杂需求方法图。

### 讲师备注

- 这页是全课真正的方法论页，应该在两个案例之后出现完整版本；前面的 P8 只展示路线。
- 动画逐层回放案例证据：原始图、稳定快照日志、Story 报告、帧 Trace、最终视频。
- 强调 AI 进阶能力的核心：**把概率性的生成放进有真相源、有反馈、有边界、有停止条件的工程系统。**

---

## 第 39 页｜离开课堂前，用七个问题判断任何 AI 复杂需求是否可开工、可继续、可交付

### PPT 内容

七问清单：

1. 用户真正要得到的结果是什么，明确不做什么？
2. 谁是业务、系统或协议的真相源？
3. 当前最大未知是什么，猜错的代价是什么？
4. 能否用一个最小穿刺先证明关键假设？
5. Story 是否有边界、依赖、禁止路径和独立验收？
6. 哪些证据能证明结果，哪些只能证明过程？
7. 失败结论、决策和下一步写回哪里，让新 Session 不再重猜？

结束语：

> 让 AI 做更多执行，让工程系统决定什么叫正确，让证据决定什么时候结束。

### 怎么做 / 怎么判断 / 不对怎么办

- 怎么做：学员选择自己的一个真实复杂需求，用七问写出一页启动卡。
- 怎么判断：七问中只要有两项无法回答，就不应该直接进入长时自治开发。
- 不对怎么办：把无法回答的项转换成澄清问题、调研任务或最小穿刺，而不是用更长 Prompt 掩盖未知。
- 本页产出：个人项目启动卡与课后实践任务。

### 讲师备注

- 最后不再复述所有工具名。邀请学员用自己的项目做迁移：一人读目标，一人挑战真相源，一人提出最小穿刺和验收证据。
- 课后作业建议：提交一份 `PRD/RFC/Story/Acceptance` 最小包，或一份 `Code Map/Spike/Story/Frame Evidence` 最小包。
- 收束时回到第一页：可信交付不是 AI 的一句“完成”，而是团队能复现的证据链。

---

# 素材与来源使用说明

## 优先使用的真实本地素材

1. SecurityTool 原始需求图、应用界面、模块设计 Diff、提交时间线。
2. 用户 ID 新增事件与旧账号集合并存的真实日志。
3. Story 验收报告、MCP 构建/安装/设备操作、测试 ID 失败报告。
4. `案例二待回填`：远控 CPU/FPS/内存/时延基线、卡顿视频和问题画面。
5. `案例二待回填`：真实 Session 纠偏、追踪日志、代码 Diff、回退记录与最终运行视频。

## 外部社区图的使用原则

- Anthropic / 官方资料用于定义 Chat、Agent、Workflow、Loop 与工具能力边界。
- Stack Overflow、METR、DORA 用于呈现行业问题和研究快照，必须保留时间与样本限制。
- Ralph、Spec-Code-Test、Verifier Loop 等社区图只作为思想参照，优先重绘并注明来源，不把社区观点写成已验证工程事实。
- 不使用无法追溯来源的社交媒体图片；如果采用 Ralph 卡通图，确认课堂传播授权或改用自绘流程。

## 主要外部来源

- https://www.anthropic.com/research/building-effective-agents
- https://www.anthropic.com/research/trustworthy-agents
- https://www.anthropic.com/webinars/future-of-ai-at-work-introducing-cowork
- https://code.claude.com/docs/en/platforms
- https://code.claude.com/docs/en/how-claude-code-works
- https://code.claude.com/docs/en/context-window
- https://www.anthropic.com/research/claude-code-expertise
- https://www.anthropic.com/research/long-running-Claude
- https://survey.stackoverflow.co/2025/ai
- https://metr.org/Early_2025_AI_Experienced_OS_Devs_Study-paper.pdf
- https://dora.dev/research/2025/dora-report/
- https://dora.dev/insights/balancing-ai-tensions/
- https://github.com/coreyepstein/ralph-methodology
- https://addyosmani.com/blog/good-spec/
- https://www.philschmid.de/inner-loop-vs-outer-loop
- https://www.langchain.com/blog/improving-deep-agents-with-harness-engineering
