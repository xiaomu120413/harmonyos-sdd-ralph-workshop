# MDM 外设管理案例｜整体审阅与落版规则

## 1. 整体评价

这版案例已经符合“复杂需求方法论”的主线：不是展示 AI 一次生成代码，而是展示如何从冲突资产中建立真源，推导分层策略，定义真实 MVVM 边界，把 Feature 拆成可验收 Story，再通过多轮失败和证据闭环收敛。

最有教学价值的不是最终开关，而是八个真实矛盾：

- 全局开关与默认策略曾被混为一谈；
- 本地单设备身份与系统类型级下发粒度不一致；
- `desiredPolicy` 与 `activePolicy` 必须分离；
- 还原不能只改本地，必须清系统残留。
- `9200010` 证明本地 allow 不等于系统没有类型 deny；
- `9200007` 证明 API 报错不等于策略完全未提交；
- Trace 已入库不等于变更通知一定发出；
- `present` 事件状态不等于设备永远真实在线。

## 2. 是否符合此前标准

| 标准 | 当前情况 | 处理 |
|---|---|---|
| 先拆需求再开发 | 已用需求卡、冲突表和优先级矩阵呈现 | 保留 |
| AI 快速熟悉代码、避免上下文爆炸 | 已写五轮渐进式资产同步 | PPT 展示索引，不塞全文件 |
| 先穿刺再定方案 | 已穿刺全局回读、系统粒度、策略冲突、旧回读 | 第 13–15 页重点讲 |
| 源码级架构 | 已映射真实 View/VM/Service/Repo/MDM | 不再使用抽象四层图 |
| 任务切分与验收点 | 8 个 Story，各有独立 AC | 第 18–19 页展示 |
| Ralph 循环有真实问题 | 15 轮真实演进、Session、错误码和 commit | 第 20–22 页展示 |
| 怎么判断 AI 对不对 | D1–D7 证据分级 + 验收矩阵 | 第 23–25 页展示 |
| 不对怎么办 | 补偿、回退、拒绝假成功、UNKNOWN、Escalate | 分散在每页“四问” |
| 证据丰富 | 文档/代码/UT/覆盖率/E2E/截图均有 | 设备视频和系统回读仍留空 |

## 3. 第 9–25 页叙事

| 页 | 作用 | 学员带走什么 |
|---:|---|---|
| 9 | 案例背景与复杂度 | 三级优先级 + 并行存储策略不是几个开关 |
| 10 | 资产同步 | 如何少读但读到真源 |
| 11 | 冲突证据 | 文档也会过期，必须交叉验证 |
| 12 | 需求卡 | 进入方案前要冻结范围与 oracle |
| 13 | 方案对比 | 先比较失败模式 |
| 14 | 最小穿刺 | 系统真实粒度改变架构 |
| 15 | ADR | 真源、所有权、失败语义 |
| 16 | MVVM 总体结构 | 真实类的职责边界 |
| 17 | 源码调用链 | UI→VM→Service→Repo/MDM |
| 18 | Story 地图 | 按可证明能力拆 |
| 19 | Worker Packet | 新 Session 如何无损接力 |
| 20 | Ralph 协议 | 一轮只收敛一个未知 |
| 21 | 真实迭代账 | 错误如何变成规则 |
| 22 | 失败处理 | 全局补偿与还原顺序 |
| 23 | 证据阶梯 | 不同证据证明不同事实 |
| 24 | E2E FAIL→PASS | 失败证据如何保留并收敛 |
| 25 | 验收矩阵 | 当前能判什么、还缺什么 |

## 4. 页内、备注、现场三层分工

### 页内

- 一个核心问题；
- 一张结构图、矩阵或证据；
- 1–3 个真实代码锚点；
- 明确结论或 `PENDING`。

### 讲师备注

- 怎么做；
- 怎么判断；
- 不对怎么办；
- 这页证据的边界。

### 现场操作

- 用 `rg` 从页面追到核心 Service；
- 打开一条 UT 解释失败分支；
- 打开 E2E JSON 读 PASS/FAIL/UNKNOWN；
- 若设备可用，展示系统回读和实物插拔。

## 5. 可直接使用的素材

- `harmonyos-sdd-workshop-media/e2e/peripheral-policy-current.png`：页面可见性。
- 源工程模块设计 2.1：策略优先级与状态表。
- `UsbGlobalPolicyService.ets`：全局事务代码。
- `UsbDevicePolicyStateService.ets`：默认/显式/拔插状态机。
- `PeripheralDevicePolicyDispatchService.ets`：系统按 baseClass 下发的限制证据。
- `PER-BL-USB-001.json` 与 `PER-POL-001.json`：FAIL→PASS。
- 2026-09-01 coverage report：证明测试执行面，同时暴露 Dispatch 低覆盖。
- `evidence/mdm/peripheral-code-test-evidence.md`：代码、测试、提交与证据边界的自包含摘要。
- `13-本地Session真实问题证据卡.md`：`9200010/9200007`、Trace 通知、在线态等真实反例和素材占位。

## 6. 必须留空的素材

没有现成证据时，以下位置保留占位，不生成假截图或假日志：

- USB 全局禁用前后系统回读；
- 默认 allow 新设备进入白名单的视频；
- 默认 deny 新设备被拒绝且进入黑名单的视频；
- 全局恢复后在线 deny 重放的视频/日志；
- 两台同 baseClass 设备的影响面验证；
- 还原前后 EDM 残留对照。
- `9200007` 部分生效后的重插结果；
- 应用未运行时拔出、重新启动后的 `present` 对账；
- Trace 写入成功但维护失败时当前 Tab 仍即时刷新的证据。

## 7. 落版禁区

1. 不再混入旧 MDM 业务案例的文字或素材。
2. 不把所有规则塞到一张抽象流程图。
3. 不把 ViewModel 画成直接调用 RDB/MDM 的“大管家”。
4. 不写“单设备精确阻断”而隐去 baseClass 粒度。
5. 不把 PASS E2E 当成系统业务验收。
6. 不删除旧 FAIL；用后续 PASS 说明问题已收敛。
7. 不把 `PENDING` 用绿色呈现。

## 8. 最终检查清单

- [x] 案例业务对象统一为外设管理。
- [x] 六阶段方法论保持不变。
- [x] MVVM 映射到真实文件和函数。
- [x] 全局、默认、显式、存储策略定义完整。
- [x] 优先级统一为“接口管控全局 > 单设备显式 > 默认黑白名单”，存储策略并行覆盖。
- [x] 动态设置、失败回退、补偿、还原写清。
- [x] 真实历史、Session、错误码、commit、UT、覆盖率和 E2E 证据已接入。
- [x] 证据边界和 PENDING 已明确。
- [ ] PPT 尚未改，本轮只更新 Markdown。
- [ ] 实物视频与系统回读待讲师补采。
