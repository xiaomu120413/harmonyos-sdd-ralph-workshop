# PPT 第 28–38 页内容映射

> 本文件是 GPU 远控案例的页码映射。完整讲稿、源码级流程图、时序图与文档证据统一以 `14-GPU案例第28-38页详细讲稿与文档证据.md` 为真源；PPT 再从真实文档取图。

## 1. 整体故事线

```text
28 问题：怎样证明卡顿路径被替换
 ↓
29 读库：源码地图压缩 55.9 万行上下文
 ↓
30 定位：源码时序找出 CPU/GPU 分叉、消费权和回退
 ↓
31 调研：迁移生命周期契约，不复制平台 ownership
 ↓
32 方案：ADR 冻结接管、owner、EndFrame 和 fallback
 ↓
33 穿刺：SP-01～05 证明一帧主链与一次回退
 ↓
34 新计划：Spike verdict 决定 CONTINUE / REPLAN / STOP
 ↓
35 拆 Story：S0～S7 按可验收行为切
 ↓
36 Worker Packet：文档、伪代码、AC、Stop 先于代码
 ↓
37 Ralph：沿第一异常循环定位和修复
 ↓
38 验收：路径、画面、性能、回退、生命周期和长稳同时闭合
```

## 2. 逐页定义

| 页 | 页面标题 | 本页只回答一个问题 | 主画面/文档截图 | 对应文档 |
|---:|---|---|---|---|
| 28 | 怎样证明卡顿路径被替换 | 为什么要改、缺什么证据 | 场景图 + 六步问题链 + before 占位 | `01`、`00`、`14` |
| 29 | 55.9 万行怎么读：先形成源码地图 | AI 怎么控制上下文 | 三入口流程 + codebase-map 截图 | `02`、`14` |
| 30 | 源码时序定位 CPU/GPU 分叉 | 旧路径、新路径与回退在哪 | Server→GFX→Bridge→Candidate→Compositor/GDI | `09`、`14` |
| 31 | 跨平台调研只迁移生命周期契约 | 为什么参考其他平台 | decoder 时序 + 调研截图 | `03`、`14` |
| 32 | ADR 冻结接管、显示和回退 | 最终选哪条路 | Bridge→Compositor→EndFrame + fallback | `04`、`14` |
| 33 | 一帧主链 + 一次回退穿刺 | 先关哪五个风险 | SP-01～05 + Spike 文档截图 | `05`、`14` |
| 34 | 穿刺后重新计划 | 为什么按 verdict 重排 | Human→Spike→Plan→Worker Packet 时序 | `05`、`14` |
| 35 | S0～S7 Ralph Story | 开发到底怎么拆 | Story 地图 + 真实 Story 截图 | `13`、`14` |
| 36 | Worker Packet 文档先行 | 新 Session 怎样不越界 | 文档先行时序 + S3 伪代码/AC | `13`、GPU `14`、MDM `14/15` |
| 37 | Ralph 沿第一异常排障 | AI 不对怎么办 | Session→Runtime→Ledger 时序 + 六步协议 | `07`、`14` |
| 38 | 工程验收矩阵 | 什么时候算交付 | Build/Path/Frame/Outcome/Fallback/Lifecycle/Soak | `00`、`06`、`14` |

## 3. 第 33–37 页的讲授链

```text
33 执行 SP-01～05 最小穿刺
   → 产物、decoder、output、present、fallback 各关一个未知
   → 缺同 run 证据时完整 Spike 仍是 PENDING

34 用穿刺真实结果生成新计划
   → 已证实的边界保留
   → 未证实的问题进入后续阶段

35 按可验收结果拆 Ralph Story
   → Plan 管顺序，Story 管行为，Worker Packet 管一次 Session
   → 每张 Story 只交付一个结果，不通过不进入下一张

36 用 Worker Packet 把文档先于代码提交
   → Read First、RED、Allowed、Forbidden、伪代码、AC、Stop 齐全
   → docs commit → code commit → evidence ledger

37 沿第一异常做 Ralph 排障循环
   → Stop → Preserve → Locate → Falsify → Repair → Replay
   → 每轮只允许一个 probe/change，并回写 Progress Ledger
```

`frameId / owner / EndFrame` 只在源码时序、Story 伪代码、证据字段和讲师解释中出现，用于证明顺序与所有权，不再成为脱离问题的抽象标题。

## 4. 缺失素材占位

| 占位 ID | 放在哪页 | 等待什么 | 未补前如何讲 |
|---|---:|---|---|
| GPU-VIDEO-BEFORE | 28/38 | CPU 路径下 20–30 秒卡顿录屏 | 展示场景图，明确视频 `PENDING` |
| GPU-PATH-BEFORE | 28 | software/CPU path 日志截图 | 使用案例背景卡，不填伪造日志 |
| GPU-SPIKE-TRACE | 33/37 | 同一次运行的硬解选择日志与失败回退记录 | 现有截图只标“画面 READY”，硬解/回退保持 PENDING |
| GPU-ISSUE-TRACE | 37 | 黑屏/色块/卡顿中一个真实问题的第一异常 | 播放现有黑屏视频，不宣称已知原因 |
| GPU-VIDEO-AFTER | 38 | 同设备/网络/分辨率/片段 after | 现有播放视频只标 `MEDIA FACT / UNBOUND` |
| GPU-PERF-A-B | 38 | CPU/FPS/frame time/queue age before/after | 数值空着，结论 `PENDING` |

## 5. 备注规则

- 每页都必须能回指一份文档和至少一个源码/证据锚点。
- 第 30 页展示源码级 SurfaceCommand→Candidate→Compositor/GDI 分叉；第 31 页再截取 `03-跨平台实现调研.md`，不使用空泛平台 Logo 墙。
- 第 32–33 页分别贴 ADR 与 Spike 真实文档；第 35 页贴 Story 文档；第 36 页贴 Worker Packet；第 37 页用第一异常表和 Progress Ledger，不使用抽象 Agent 角色图。
- CPU 路径与卡顿原因按用户冻结背景讲述；具体数值、录屏、路径日志和 after 结果没有资料时必须保留占位。
