# PPT 第 28–38 页内容映射

> 本文件是 GPU 远控案例的页码真源。文档、时序图、任务卡和证据占位先在 Markdown 中形成，PPT 再从真实文档取图。第 29–33 页还必须说明文档的输入、AI 动作、Human Review、用途与下一阶段入口，不能让产物凭空出现。

## 1. 整体故事线

```text
28 背景：卡顿，已知原因是 CPU/软件路径、未走 GPU
 ↓
29 读库：不读 55.9 万行，只回答五个导航问题
 ↓
30 调研：对照 FFmpeg / OpenH264 / MF / MediaCodec / OH_AVCodec，提炼契约
 ↓
31 方案：确认 RDPGFX 受控接管 + OH_AVCodec + GPU + original GDI fallback
 ↓
32 穿刺：只证明一帧输入 → native output → EndFrame present → fallback
 ↓
33 拆分：把方案拆成 T00–T07，每任务先写验收和 Stop
 ↓
34 开发：真正做 T01–T03，先接 hook/硬解，再通 buffer，最后交给 GPU owner
 ↓
35 循环：每轮只收敛一个未知，证据写回 progress，再决定下一轮
 ↓
36 解题：遇到黑屏/色块/仍卡顿，沿同一 frame 找第一处异常
 ↓
37 交付：补齐队列、生命周期、AVC444、fallback、回归与证据包
 ↓
38 验收：before/after 视频 + 路径日志 + CPU/FPS + 长稳矩阵；缺失项保留占位
```

## 2. 逐页定义

| 页 | 页面标题 | 本页只回答一个问题 | 主画面/文档截图 | 对应文档 |
|---:|---|---|---|---|
| 28 | 背景：CPU 路径让远控视频卡顿 | 为什么要改 | before 视频占位 + 背景卡 + CPU path 日志占位 | `01-问题与基线.md` |
| 29 | `02-codebase-map` 怎么生成 | AI 怎么只找有用信息 | Input/AI/Human/Why 卡 + 五问 + 真实文档摘录 | `02-大代码库认知地图.md`、`09`、`11` |
| 30 | `03-platform-research` 怎么生成 | 别的平台哪些契约能复用 | 生成链 + 真实调研文档截图 + 平台对照 | `03-跨平台实现调研.md`、`11` |
| 31 | `ADR-GPU-001` 为什么成立 | 最终选哪条路，为什么 | 调研输入 → 候选 → Human Decision → Decision/Deferred/Fallback | `04-ADR-GPU-001-HarmonyOS硬解方案.md`、`11` |
| 32 | `05-spike-plan` 从哪里来 | 哪五个风险必须先关掉 | ADR Evidence Gate → SP-01–05 + 真实计划截图 | `05-最小能力穿刺计划.md`、`11` |
| 33 | Task Card 怎样从穿刺推导 | 开发到底怎么拆 | 穿刺结论 → 一个可观察结果 → Allowed/Forbidden/Verify/Stop | `tasks/`、`09` 第 6 节、`11` |
| 34 | 实际开发：先打通一帧主链 | AI 真正改哪些地方 | T01 hook/decoder → T02 buffer → T03 owner/EndFrame；每段附验收 | T01/T02/T03 任务卡、`09` 第 2–4 节 |
| 35 | Ralph 循环：一轮只收敛一个未知 | 开发不是一次聊天，如何连续推进 | R1–R5 运行账 + progress 截图 | `07-开发排障复盘.md`、`evidence/gpu/03-*` |
| 36 | 遇到黑屏、色块、仍卡顿怎么办 | AI 不对时怎么定位并修正 | 三个现象的第一观测 + Stop/Preserve/Locate/Falsify/Repair/Replay | `07-开发排障复盘.md`、`09` 第 8 节 |
| 37 | 工程化与最终交付 | 一帧通了之后还缺什么 | T04 队列、T05 生命周期、T06 AVC444、T07 验收 + 交付包目录 | `06-工程验收计划.md`、T04–T07 |
| 38 | 结果验收与视频 | 什么证据到了才能说交付 | before/after 视频、路径、CPU/FPS、frame trace、fallback、soak；缺失项空着 | `00-证据状态总表.md`、`06-工程验收计划.md` |

## 3. 第 33–36 页的讲授链

在进入任务之前，第 29–33 页每页都先用同一张文档生成卡回答：

```text
Document → Input → AI action → Human decision → Evidence → Why → Next
```

这条说明只占页面一小块，但必须出现。没有它，调研、ADR、Spike 和 Task Card 会像 AI 突然给出的答案，学员无法迁移方法。

### 第 33 页｜先拆任务

```text
穿刺证实了边界
  → 按风险闭环拆 T00–T07
  → 每张卡写 Allowed / Forbidden / Verify / Stop
  → 进入门没过，不开始下一任务
```

### 第 34 页｜再看实际开发

```text
T01 先把 bridge 和 hardware decoder 真正带进产物/设备
  → T02 证明一份压缩输入产生合法 native output
  → T03 证明 owner 唯一且只在 matched EndFrame present
```

这页不画人/多 Agent 抽象泳道，直接展示“改哪个符号—得到什么证据—不通在哪里停”。

### 第 35 页｜把开发写成运行账

| 轮次 | 唯一未知 | 本轮证据 | 下一步 |
|---|---|---|---|
| R1 | bridge 是否进入目标 HAP | symbol / library identity | 只有 PASS 才进 R2 |
| R2 | 真机是否选硬件 decoder | capability / decoder name | 不支持则停在 UNSUPPORTED |
| R3 | input/output 是否可关联 | frameId/PTS/format/stride/planes | 输出合法才进显示 |
| R4 | owner/EndFrame 是否正确 | pendingFrameId/matched present | 黑屏则找第一处异常 |
| R5 | 连续播放是否有界 | queue depth/age/drop/CPU/FPS | 决定队列和背压任务 |

### 第 36 页｜遇到问题只修第一处异常

| 现象 | 先看什么 | 不要先做什么 |
|---|---|---|
| decoder 有 output 但黑屏 | native import → composite → pending → matched EndFrame → target generation | 直接猜 shader |
| 粉/绿色块或花屏 | format / stride / planes / rect / retained background | 盲改色彩矩阵 |
| 刚开始流畅，随后变卡 | queue depth / age p95 / decodeUs / present gap | 直接增加线程 |

## 4. 缺失素材占位

| 占位 ID | 放在哪页 | 等待什么 | 未补前如何讲 |
|---|---:|---|---|
| GPU-VIDEO-BEFORE | 28/38 | CPU 路径下 20–30 秒卡顿录屏 | 展示场景图，明确视频 `PENDING` |
| GPU-PATH-BEFORE | 28 | software/CPU path 日志截图 | 使用案例背景卡，不填伪造日志 |
| GPU-SPIKE-TRACE | 32/34 | decoder/frame/owner/EndFrame 同帧 trace | 显示字段空表 |
| GPU-ISSUE-TRACE | 36 | 黑屏/色块/卡顿中一个真实问题的第一异常 | 播放现有黑屏视频，不宣称已知原因 |
| GPU-VIDEO-AFTER | 38 | 同设备/网络/分辨率/片段 after | 现有播放视频只标 `MEDIA FACT / UNBOUND` |
| GPU-PERF-A-B | 38 | CPU/FPS/frame time/queue age before/after | 数值空着，结论 `PENDING` |

## 5. 备注规则

- 每页都必须能回指一份文档和至少一个源码/证据锚点。
- 第 30 页优先截取 `03-跨平台实现调研.md` 的对照表和时序图，不使用空泛平台 Logo 墙。
- 第 33–36 页优先截取 Task Card、progress ledger 和排障表，不使用抽象 Agent 角色图。
- CPU 路径与卡顿原因按用户冻结背景讲述；具体数值、录屏、路径日志和 after 结果没有资料时必须保留占位。
